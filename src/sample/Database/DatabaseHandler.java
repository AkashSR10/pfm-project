package sample.Database;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.SQLException;

public class DatabaseHandler extends Connect {

//    Connection conn;

  //  public void signUpUser(String first_name, String last_name, String email, String password, String gender) {
    //    String insert = "INSERT INTO " + Const.MEMBER_TABLE + "(" + Const.MEMBER_FIRSTNAME + ","
    //            + Const.MEMBER_LASTNAME + ", " + Const.MEMBER_EMAIL + "," + Const.MEMBER_PASSWORD + "," + Const.MEMBER_GENDER + ")" + "VALUES(?,?,?,?,?)";

      //  try {
       // } catch (SQLException e) {
       //     e.printStackTrace();
      //  }
  //  }
}